# Copier en .env avant le premier démarrage :  cp .env.example .env
# .env ne doit jamais être versionné ni déposé avec le rapport.

POSTGRES_DB=veille
POSTGRES_USER=veille
POSTGRES_PASSWORD=changez_moi

# Clés d'API des modèles — renseignées dans n8n (Credentials), pas ici.
# Rappelées pour mémoire : OpenAI, Anthropic, Google AI Studio, Perplexity.
# Le pipeline hard data (pilote A5) n'en requiert aucune : c'est précisément le propos du § 13.1.
